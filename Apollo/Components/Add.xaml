<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Canvas Margin="5 0" HorizontalAlignment="Center" Width="16" Height="16" Background="Transparent" x:Name="Icon" PointerReleased="Clicked">
    <Canvas.ContextMenu>
      <ContextMenu>
        <MenuItem Header="Delay" />
      </ContextMenu>
    </Canvas.ContextMenu>

    <Path Stroke="#DCDCDC" StrokeThickness="2"
          Data="M 0,8 L 16,8 M 8,0 L 8,16" />
  </Canvas>
</UserControl>