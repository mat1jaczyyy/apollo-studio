<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*" />
      <RowDefinition Height="2*" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Dial" />

    <Canvas Grid.Row="1" Width="50">
      <Path Stroke="#0A0A0A" StrokeThickness="7"  
        Data="M 10.75,40.12 A 18,18 300 1 1 32.25,40.12" />
    </Canvas>

    <NumericUpDown Grid.Row="2" />
  </Grid>
</UserControl>