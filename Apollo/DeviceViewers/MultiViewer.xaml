<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Class="Apollo.DeviceViewers.MultiViewer"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:Components="clr-namespace:Apollo.Components">
  
  <Grid RowDefinitions="*,Auto">
    <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Visible" HorizontalScrollBarVisibility="Auto">
      <StackPanel x:Name="Contents" Margin="10 5">
        <Components:VerticalAdd AlwaysShowing="true" Added="Chain_InsertStart" x:Name="ChainAdd" />
      </StackPanel>
    </ScrollViewer>
  
    <Border Grid.Row="1" Margin="10 0 10 5" BorderBrush="{DynamicResource ThemeBorderHighBrush}" BorderThickness="0 1 0 0">
      <ComboBox Margin="0 5 0 0" BorderThickness="0" Background="Transparent" HorizontalAlignment="Center" x:Name="ComboBox" SelectionChanged="Mode_Changed">
        <sys:String>Forward</sys:String>
        <sys:String>Backward</sys:String>
        <sys:String>Random</sys:String>
        <sys:String>Random+</sys:String>
      </ComboBox>
    </Border>
  </Grid>
</UserControl>