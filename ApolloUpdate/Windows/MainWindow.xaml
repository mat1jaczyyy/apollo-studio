<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Update.Windows.MainWindow"
        xmlns:Components="clr-namespace:Update.Components"
        Title="Apollo Studio" Background="{DynamicResource ThemeSplashBrush}" Topmost="true" WindowStartupLocation="CenterScreen"
        Icon="/Resources/WindowIcon.png"
        Width="640" MinWidth="640" MaxWidth="640"
        Height="320" MinHeight="320" MaxHeight="320"
        Opened="Loaded" Closing="Unloaded">

  <Border BorderBrush="{DynamicResource ThemeBorderLowBrush}" BorderThickness="1">
    <Grid RowDefinitions="Auto,*">
      <Grid Grid.Row="0" ColumnDefinitions="*,Auto">
        <Grid Grid.Column="0" Background="Transparent" PointerPressed="MoveWindow" />
        <Components:Close Grid.Column="1" Margin="5 5" Clicked="Close" />
      </Grid>

      <Grid Grid.Row="1" ColumnDefinitions="*,*" Margin="10 0 10 10">
        <Grid Grid.Column="0" x:Name="Root" />
      </Grid>
    </Grid>
  </Border>
</Window>